package main;

public class Fitness {
    Generator generator = new Generator();
    Validator validator = new Validator();

    public static float fitnessFunction(int[][] solution, int[][] matrix) {
        return 0;
    }
}
